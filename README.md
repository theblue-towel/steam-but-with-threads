Webscrapes steam top 50 games, then uses threading to submit everything to a google form using selenium.

YOU NEED TO MAKE A .env, then add variable 'form_link' and set it to the google form link. 
Then you could open the responses and view it in a sheet.

THIS WILL PROBABLY CRASH YOUR COMPUTER since it does open 50 instances of chrome (and quickly closes them).
